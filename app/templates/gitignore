.DS_Store
.idea
npm-debug.log
/bower_components
/node_modules
/test/coverage

## /dist
## Rather than ignore the /dist folder, mark it as "unchanged" so you can check it in when needed:
## > git ls-files -z dist/ | xargs -0 git update-index --assume-unchanged
##
## To view a list of files marked "assume-unchanged": git ls-files -v
##
## To undo this and recurively mark the dist folder as "changed":
## > git ls-files -z dist/ | xargs -0 git update-index --no-assume-unchanged