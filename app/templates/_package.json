{
  "name": "<%= _.slugify(libname) %>",
  "version": "0.0.0",
  "repository": "http://github.com/kiva/<%= _.slugify(libname) %>",
  "licenses": [
    {
	  "type" : "MIT",
	  "url" : "http://github.com/kiva/<%= _.slugify(libname) %>/license.txt"
    }
  ],
  "scripts": {
	"test": "grunt test --verbose",
    "prepublish": "bower install  && git init"
  },
  "dependencies": {},
  "devDependencies": {
	"bower": "~1.3.5",
	"buster": "~0.7.13",
	"buster-coverage": "0.1.1",
	"coveralls": "2.10.1",
    "grunt": "~0.4.5",
	"grunt-buster": "0.3.1",
	"grunt-contrib-jshint": "0.10.0",
	"grunt-contrib-uglify": "~0.5.0",
	"grunt-rigger": "0.6.0",
	"grunt-shell": "0.7.0"
  }
}
