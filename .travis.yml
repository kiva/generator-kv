language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-kv' ]; then cd .. && eval "mv $currentfolder generator-kv" && cd generator-kv; fi

